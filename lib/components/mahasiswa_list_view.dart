
import 'package:flutter/material.dart';

class MahasiswaListView extends StatelessWidget {
  const MahasiswaListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
